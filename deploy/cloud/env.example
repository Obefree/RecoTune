# Copy to repo root as .env.cloud (see deploy/cloud/README.md)
#
# Get a token: https://dashboard.ngrok.com/get-started/your-authtoken
# Improves tunnel reliability; required for some ngrok features.
NGROK_AUTHTOKEN=

# Optional: Metro port inside the container (default 8081).
# EXPO_PORT=8081
