# Практика: модал «База песен» — layout

**Дата:** 2026-05-22  
**Файл:** `src/screens/ChordsScreen.tsx`

Шапка модалки без `flexDirection: 'row'` складывала кнопки столбиком; `FlatList` без `flex: 1` не занимал остаток экрана — снизу чёрная пустота. Исправлено: компактная шапка (`libModalChrome`), список `libList` на всю высоту, SafeArea снизу; тот же модал из кнопки «База» и по тапу на аккорд в панели практики.
