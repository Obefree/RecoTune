# Stem separation (Demucs) — install in venv, see README.md
demucs>=4.0.0
torch>=2.0.0
torchaudio>=2.0.0

# Melody transcription (P2) — optional, heavy (TensorFlow); can use separate venv
basic-pitch>=0.3.0
